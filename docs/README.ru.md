# 0xNEKO

<div align="center">

**ERC20 токен с майнингом на основе Proof-of-Work, оптимизированный для CPU**

[Функции](#функции) • [Быстрый старт](#быстрый-старт) • [Справочник API](#справочник-api) • [Руководство по майнингу](#руководство-по-майнингу) • [Безопасность](#безопасность)

</div>

---

## Обзор

0xNEKO — это децентрализованный майнинг-токен ERC20, использующий новый алгоритм Proof-of-Work **NekoCycle**. Процесс майнинга включает поиск циклов в структуре графа, где генерация рёбер использует **BranchingHash** — алгоритм ветвления, зависящий от данных, оптимизированный для CPU-майнинга.

### Ключевые особенности

- 🔒 **Полностью децентрализован** — Нет ключей администратора, нет привилегий владельца
- ⛏️ **Оптимизация для CPU** — BranchingHash использует ветвление, зависящее от данных
- 🔗 **Привязка к сети** — Решения привязаны к конкретному Chain ID и адресу майнера
- 📈 **Сложность ASERT** — Плавная экспоненциальная регулировка сложности
- 💰 **Плавная эмиссия** — Кривая вознаграждения с постепенным затуханием и хвостовой эмиссией

---

## Токеномика

| Параметр | Значение |
| :--- | :--- |
| **Название токена** | 0xNEKO |
| **Символ** | 0xNEKO |
| **Десятичные знаки** | 18 |
| **Максимальное предложение** | 1 000 000 000 (1 миллиард) |
| **Начальная награда** | ~953.67 NEKO |
| **Коэффициент скорости эмиссии** | 20 |
| **Хвостовая эмиссия** | Минимум 0.1 NEKO |
| **Целевое время блока** | 60 секунд |

---

## Функции

### NekoCycle Proof-of-Work

Алгоритм майнинга требует нахождения **цикла длиной 42** в двудольном графе, где рёбра генерируются функцией `_branchingHash()`.

### Регулировка сложности ASERT

| Параметр | Значение | Описание |
| :--- | :--- | :--- |
| Период полураспада | 2 880 блоков | ~2 дня при целевой скорости |
| Целевое время блока | 60 секунд | 1 минута между блоками |
| Обновление якоря | Каждые 100 блоков | Предотвращает дрейф расчётов |

### Плавная кривая эмиссии

```
награда = (максПредложение - добытоТокенов) >> коэффициентЭмиссии
```

---

## Быстрый старт

```bash
# Клонировать репозиторий
git clone https://github.com/your-repo/0xneko.git
cd 0xneko

# Установить зависимости
npm install

# Скомпилировать контракты
npx hardhat compile

# Запустить тесты
npx hardhat test

# Развернуть
npx hardhat run scripts/deploy.js --network hardhat
```

---

## Справочник API

### Функции майнинга

#### `mint(uint256 nonce, uint256[] calldata solution)`
Отправить действительное решение для майнинга и выпустить токены.

### Функции запросов

| Функция | Описание |
| :--- | :--- |
| `getBlockInfo()` | Возвращает всю информацию о майнинге за один вызов |
| `getChallengeNumber()` | Текущий хеш задачи |
| `getMiningTarget()` | Целевая сложность |
| `getMiningReward()` | Текущая награда за блок |
| `getLocalChallenge(miner, nonce)` | Получить локальную задачу |
| `verifySolution(nonce, solution)` | Предварительная проверка решения |
| `computeEdge(root, edgeIndex)` | Вычислить ребро (u, v) |
| `getDifficulty()` | Читаемая сложность |
| `getDifficultyInfo()` | Параметры ASERT |
| `getEconomicsInfo()` | Информация о токеномике |
| `getNetworkStats()` | Оценка хешрейта сети |
| `getConstants()` | Константы протокола |

---

## Руководство по майнингу

```
1. Получить текущую задачу: getChallengeNumber()
2. Сгенерировать локальную задачу: hash(challenge, miner, nonce, chainid)
3. Построить рёбра графа с помощью computeEdge()
4. Найти 42-цикл в графе
5. Проверить хеш решения < цели
6. Отправить: mint(nonce, solution)
```

---

## Стоимость Gas

| Операция | Gas |
| :--- | ---: |
| **Развёртывание контракта** | ~1 390 000 |
| **Полная транзакция Mint** | ~250 000 |

---

## Безопасность

| Защита | Реализация |
| :--- | :--- |
| **Реентрантность** | OpenZeppelin ReentrancyGuard |
| **Паттерн CEI** | Обновление состояния перед _mint() |
| **Защита от переполнения** | ASERT использует unchecked+предварительная проверка |
| **Привязка к сети** | Решения включают block.chainid |
| **Без администратора** | Полностью децентрализован |

---

## Лицензия

MIT
